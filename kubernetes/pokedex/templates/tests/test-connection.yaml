apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pokedex.fullname" . }}-test-connection"
  labels:
    {{- include "pokedex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pokedex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
